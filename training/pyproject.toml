[project]
name = "training"
version = "0.1.0"
description = "YOLO training pipeline with Ultralytics and MLflow"
requires-python = ">=3.11"
dependencies = [
    "ultralytics>=8.3",
    "mlflow>=2.10",
    "pyyaml>=6.0",
    "numpy>=1.26",
    "scikit-learn>=1.8.0",
]
